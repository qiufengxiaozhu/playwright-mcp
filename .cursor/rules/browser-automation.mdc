---
description: 浏览器自动化操作的前置知识和最佳实践，当用户要求使用浏览器操作MCP进行网页交互时自动应用
globs:
alwaysApply: false
---

# 浏览器自动化操作前置知识

## 核心原则

### 1. 操作方式优先级

本项目有四种浏览器自动化方式，**按优先级从高到低**：

| 优先级 | 方式 | 说明 | 适用场景 |
|--------|------|------|----------|
| **1（最高）** | `user-browser-use` MCP | Playwright MCP 服务器，Cursor 直接驱动，逐步交互 | 所有需要浏览器操作的场景（推荐） |
| **2** | Python + Playwright 逐步交互 | 当 MCP 不可用时，用 Python 逐步调用 Playwright | MCP 未注册时的备选 |
| **3** | browser-use Agent（需 OpenAI/Anthropic/Gemini） | LLM 自主决策操作浏览器 | 复杂多步骤任务（需兼容 LLM） |
| **4（最低）** | `cursor-ide-browser` | IDE 内置浏览器 | 仅用于 Canvas 可视化、本地前端预览 |

> **关键原则**：
> - **默认使用外部浏览器**，非不得已不使用 `cursor-ide-browser`
> - **逐步交互优于自动化脚本**：每一步都主动识别页面元素、截图确认，而非写死流程脚本
> - 使用 Playwright 备选时，也应逐步执行命令，每步截图观察，而非一次性跑完整脚本
> - **MiniMax 不兼容 browser-use Agent**（不支持 Structured Output），仅可用于 langchain 模式

### 2. 截图与结果记录

- **每完成一个任务**，必须将结果截图保存到桌面（`/mnt/c/Users/lenovocloud/Desktop/`），图片命名为任务名
- 过程中的关键步骤也可以截图记录（保存到 `/tmp/screenshots/` 即可）
- 截图路径确认：先检查 `/mnt/c/Users/` 下的实际用户名

### 3. 前置知识实时同步 GitHub

每次修改本文件后，**必须立即同步推送到 GitHub**：

```bash
cd /opt/playwright-mcp
cp .cursor/rules/browser-automation.mdc browser-automation.mdc
git add browser-automation.mdc .cursor/rules/browser-automation.mdc
echo "提交信息" > /tmp/commit_msg.txt && /usr/bin/git commit -n -F /tmp/commit_msg.txt
git push origin main
```

操作中积累的新经验、踩坑记录，应主动补充到本文件并推送。

## `user-browser-use` MCP 工具使用指南

MCP 服务器名称在 Cursor 中注册为 `user-browser-use`（取决于 `.cursor/mcp.json` 配置）。

### 工具清单

| 工具 | 用途 | 关键参数 |
|------|------|----------|
| `browser_open` | 打开浏览器并导航到 URL | `url`(必填), `wait_until` |
| `browser_screenshot` | 截图查看当前页面状态 | `wait_for_idle`, `wait_ms`, `full_page` |
| `browser_snapshot` | 获取页面 DOM 结构化快照 | `selector`(限定范围), `include_text_nodes` |
| `browser_click` | 点击元素 | `selector` 或 `text`, `force`, `timeout` |
| `browser_type` | 在输入框中输入文字 | `selector`(必填), `text`(必填), `clear` |
| `browser_select` | 下拉框选择 | `selector`(必填), `value` 或 `label` |
| `browser_upload_file` | 上传文件到 file input | `selector`(必填), `file_path`(必填) |
| `browser_upload_via_dialog` | 通过拦截文件对话框上传 | `file_path`(必填), `trigger_selector` 或 `trigger_text` |
| `browser_scroll` | 滚动页面 | `direction`, `amount`, `selector` |
| `browser_wait_for` | 等待条件满足 | `selector`, `text`, `idle`, `timeout` |
| `browser_execute_js` | 执行 JavaScript | `script`(必填) |
| `browser_get_text` | 获取文本内容 | `selector`(可选) |
| `browser_close` | 关闭浏览器 | 无参数 |

### 标准操作流程（逐步交互）

```
1. browser_open → 打开目标 URL
2. browser_screenshot → 确认页面加载状态
3. browser_snapshot → 获取可交互元素，找到 CSS 选择器
4. 根据识别结果决定操作 → browser_click / browser_type / ...
5. browser_screenshot → 确认操作结果
6. 重复 3-5 直到完成
7. 任务完成时 → 截图保存到桌面
```

### 关键注意事项

1. **先截图/快照再操作**：每次交互前确认页面状态
2. **CSS 选择器优先**：用 `browser_snapshot` 获取选择器，比文本匹配更可靠
3. **文件上传**：优先用 `browser_upload_via_dialog`（更通用），`browser_upload_file` 仅在有可见 file input 时用
4. **文件路径**：使用 WSL 路径格式（如 `/tmp/test.docx`）
5. **下拉框**：原生 `<select>` 用 `browser_select`；自定义组件先 click 打开再 click 选项

## Playwright 备选方案（当 MCP 不可用时）

当 `user-browser-use` MCP 未注册到 Cursor 时，用 Python + Playwright **逐步交互**。

Python 环境：`/opt/playwright-mcp/.venv/bin/python`

### 逐步交互模式（推荐）

不要写一次性跑完的大脚本，而是每一步单独执行 Python 命令，观察结果后再决定下一步：

```bash
/opt/playwright-mcp/.venv/bin/python -c "
import asyncio
from playwright.async_api import async_playwright
async def step():
    pw = await async_playwright().start()
    browser = await pw.chromium.launch(headless=False, args=[
        '--no-sandbox','--disable-setuid-sandbox','--disable-gpu',
        '--disable-dev-shm-usage','--dns-prefetch-disable',
        '--disable-features=NetworkService,NetworkServiceInProcess'
    ])
    ctx = await browser.new_context(viewport={'width':1920,'height':1080}, locale='zh-CN')
    page = await ctx.new_page()
    await page.goto('目标URL', wait_until='domcontentloaded', timeout=60000)
    await asyncio.sleep(3)
    await page.screenshot(path='/tmp/screenshots/step_name.png')
    await ctx.storage_state(path='/tmp/browser_state.json')
    await browser.close()
    await pw.stop()
asyncio.run(step())
"
```

### 关键技巧

1. **登录后保存 storage_state**：`await ctx.storage_state(path="/tmp/browser_state.json")`，后续复用
2. **卡片点击可能打开新标签页**：用 `ctx.expect_page()` 捕获
3. **SPA 页面无隐藏 file input**：用 `page.expect_file_chooser()` + 点击上传区域
4. **Fluent UI ChoiceField**：通过 `page.locator("label", has_text="选项文本").click()` 选择
5. **JSON 配置 textarea**：先 `input_value()` 读取 → `json.loads()` 解析修改 → `fill()` 回填
6. **获取页面元素**：用 `page.evaluate()` 执行 JS 扫描 DOM，比 accessibility tree 更可靠

## 常见失败场景与解决方案

| 场景 | 原因 | 解决 |
|------|------|------|
| 元素找不到 | 页面未加载完 / 选择器错误 | 先截图确认，再 snapshot 获取正确选择器 |
| 文件上传无反应 | 页面无可见 file input | 改用 `browser_upload_via_dialog` |
| 自定义下拉框选择失败 | 不是原生 `<select>` | 先 click 打开下拉，再 click 选项 |
| cursor-ide-browser accessibility tree 为空 | SPA 框架限制 | 改用外部浏览器 |
| Playwright Chromium 导航超时 | 代理/VPN 环境 DNS 问题 | 添加 `--dns-prefetch-disable --disable-features=NetworkService,NetworkServiceInProcess` 启动参数 |
| `page.screenshot()` 超时 | 字体加载卡住 | 改用 `page.locator('body').screenshot()` 或拦截字体请求 |
| 弹窗遮罩层阻挡操作 | 登录弹窗等 | 用 JS `document.querySelectorAll('[to="body"],.reds-mask').forEach(el=>el.remove())` 移除 |
| 小红书搜索结果不显示 | 未登录限制 | 需登录或从 `__INITIAL_STATE__` SSR 数据提取 |
| `wait_until='networkidle'` 超时 | 页面有持续网络请求 | 改用 `domcontentloaded` 或 `commit` |

## WSL + 代理环境 Chromium 网络问题（已验证 2026-03-19）

### 问题现象

在 WSL 环境下使用 Surge/Clash 等代理软件（fake-ip 模式，DNS 解析返回 198.18.0.x），Playwright 的 Chromium 浏览器会出现：
- `curl` 正常（0.1-0.5秒），但 Chromium `page.goto()` 超时（60-120秒）
- `ping` 正常，HTTPS 连接间歇性失败
- 网络时好时坏，不稳定

### 根因

Chromium 的 NetworkService 进程与代理软件的虚拟网络接口冲突，导致 DNS 预取和连接建立失败。

### 解决方案

**必须在 Chromium 启动参数中添加以下两个 flag**：

```python
browser = await pw.chromium.launch(
    headless=False,
    args=[
        '--no-sandbox',
        '--disable-setuid-sandbox',
        '--disable-gpu',
        '--disable-dev-shm-usage',
        '--dns-prefetch-disable',                                    # 关键！
        '--disable-features=NetworkService,NetworkServiceInProcess',  # 关键！
    ]
)
```

这两个参数已内置到 `mcp_server.py` 中，MCP 模式下无需额外配置。Python 逐步交互模式下需手动添加。

### 网络不稳定时的应对策略

1. **先用 curl 测试**：`curl -s --connect-timeout 5 -o /dev/null -w "%{http_code}" https://目标网站`
2. **超时后等待重试**：代理环境网络波动是常态，等 30-60 秒后重试
3. **优先用 `domcontentloaded`**：比 `networkidle` 更快完成，避免等待所有网络请求
4. **截图用 locator**：`page.locator('body').screenshot()` 比 `page.screenshot()` 更不容易因字体加载卡住

## GitHub 仓库信息

- **仓库地址**: https://github.com/qiufengxiaozhu/playwright-mcp.git
- **分支**: main
- **规则文件位置**: `browser-automation.mdc` 和 `.cursor/rules/browser-automation.mdc`
- **提交命令**: `/usr/bin/git commit -n -F /tmp/commit_msg.txt`（`-n` 绕过旧版 git 2.25 hook 兼容性问题）

## MCP 服务器配置参考

项目提供了 `mcp.json.example` 作为初始化模板。使用时复制到 `.cursor/mcp.json`：

```bash
cp mcp.json.example .cursor/mcp.json
```

默认配置（可视化外部浏览器）：

```json
{
  "mcpServers": {
    "user-browser-use": {
      "command": "/opt/playwright-mcp/.venv/bin/python",
      "args": ["/opt/playwright-mcp/mcp_server.py"],
      "env": {
        "BROWSER_MODE": "visible",
        "VIEWPORT_WIDTH": "1920",
        "VIEWPORT_HEIGHT": "1080",
        "BROWSER_LOCALE": "zh-CN",
        "BROWSER_TIMEOUT": "30000"
      }
    }
  }
}
```

环境变量：`BROWSER_MODE`(visible/headless)、`VIEWPORT_WIDTH`(1920)、`VIEWPORT_HEIGHT`(1080)、`BROWSER_LOCALE`(zh-CN)、`BROWSER_TIMEOUT`(30000)

## browser-use Agent 模式

browser-use 是基于 Playwright 之上的 AI 浏览器代理框架，让 LLM 自主理解页面并决策操作。

### ⚠️ MiniMax 兼容性问题（已验证 2026-03-19）

**MiniMax M2.7 目前无法直接驱动 browser-use Agent**，原因：

1. **不支持 Structured Output（json_schema）**：browser-use 0.12.2 依赖 OpenAI 的 `response_format: json_schema` 来约束 LLM 输出为结构化的 `AgentOutput` 对象（包含 `action` 数组等字段）。MiniMax 不支持此功能。
2. **`<think>` 标签干扰**：MiniMax M2.7 是推理模型，即使指定 `response_format: json_object`，返回内容仍以 `<think>...</think>` 标签开头，导致 JSON 解析失败。
3. **适配尝试**：创建了 `MiniMaxChat` 子类继承 `browser_use.llm.openai.chat.ChatOpenAI`，在 `ainvoke` 中剥离 `<think>` 标签后解析 JSON。部分步骤能成功解析，但 MiniMax 的输出格式不够稳定（有时输出纯文本而非 JSON），导致频繁失败。

**结论**：browser-use Agent 需要支持 Structured Output 的 LLM，推荐：
- **OpenAI GPT-4o / GPT-4o-mini**（原生支持 json_schema）
- **Anthropic Claude**（通过 tool_use 实现结构化输出）
- **Google Gemini**（支持 response_schema）
- **Browser Use 官方模型**（专为 browser-use 优化）

### browser-use 0.12.2 版本关键变化

- **不再使用 langchain**：自带 `browser_use.llm.openai.chat.ChatOpenAI`（dataclass），直接使用 `openai` SDK
- **`BrowserConfig` 已更名为 `BrowserProfile`**，`Browser` 实际是 `BrowserSession`
- **LLM 必须实现 `provider` 属性**（返回字符串如 `'openai'`）和 `model` 属性
- **`TokenCost.register_llm()` 会 monkey-patch `ainvoke`**：通过 `setattr(llm, 'ainvoke', ...)`，Pydantic 模型需要 `extra='allow'`

### 配置 LLM API Key

API key 通过环境变量配置，**不要写死在代码中**：

```bash
# 推荐：OpenAI（browser-use 原生支持最好）
export OPENAI_API_KEY=sk-xxx

# 或 MiniMax（仅用于 test_agent.py 中的 langchain 模式，不兼容 browser-use Agent）
export MINIMAX_API_KEY=sk-cp-xxx

# 写入 .env 文件（已被 .gitignore 忽略，不会泄露）
echo "OPENAI_API_KEY=sk-xxx" >> /opt/playwright-mcp/.env
```

### 支持的 LLM 提供商

| 环境变量 | 提供商 | 模型 | browser-use 兼容 |
|----------|--------|------|------------------|
| `OPENAI_API_KEY` | OpenAI | gpt-4o-mini | ✅ 完全兼容 |
| `GOOGLE_API_KEY` | Google | gemini-2.0-flash | ✅ 兼容 |
| `ANTHROPIC_API_KEY` | Anthropic | claude-sonnet | ✅ 兼容 |
| `BROWSER_USE_API_KEY` | Browser Use | 官方模型 | ✅ 专门优化 |
| `MINIMAX_API_KEY` | MiniMax | MiniMax-M2.7 | ❌ 不兼容（无 Structured Output） |

## 小红书（xiaohongshu.com）自动化经验

### 登录状态持久化（已验证 2026-03-20）

小红书对未登录用户严格限制内容访问（搜索结果为空、笔记详情被遮罩），**必须登录后操作**。

#### 登录状态保存与复用

1. **保存路径**：`/opt/browser-use-demo/xhs_storage_state.json`
2. **保存方式**：用户手动在非 headless 浏览器中登录后，通过 `ctx.storage_state()` 保存
3. **复用方式**：后续创建 browser context 时加载：
   ```python
   ctx = await browser.new_context(
       storage_state='/opt/browser-use-demo/xhs_storage_state.json',
       viewport={'width':1920,'height':1080},
       locale='zh-CN'
   )
   ```
4. **有效期**：`web_session` cookie 有效期有限，过期后需重新登录

#### 登录流程

1. 启动非 headless Chromium（`headless=False`），打开 `https://www.xiaohongshu.com/explore`
2. **提醒用户手动登录**（扫码或手机验证码）
3. 等待用户确认后，执行 `ctx.storage_state()` 保存
4. 验证：用保存的 state 打开新 context，访问 explore 页面确认有内容

> **重要提醒**：下次需要操作小红书时，先检查 `xhs_storage_state.json` 是否存在且未过期。如果过期或不存在，需要**提醒用户手动登录**。

### 小红书页面特点

- **登录弹窗**：CSS class 包含 `login-container`、`mask-paper`，可用 JS 移除：
  ```javascript
  document.querySelectorAll('[class*="login-container"],[class*="mask-paper"],[class*="reds-mask"]').forEach(el=>el.remove())
  ```
- **搜索页面**：`https://www.xiaohongshu.com/search_result?keyword=关键词&source=web_search_result_notes`
- **发现页面**：`https://www.xiaohongshu.com/explore`（有频道分类：推荐、穿搭、美食、旅行等）
- **SSR 数据**：部分页面在 `window.__INITIAL_STATE__` 中有服务端渲染数据，可通过 `page.evaluate()` 提取
- **反爬策略**：频繁请求可能触发验证码，建议操作间隔 3-5 秒

### 桌面路径

Windows 桌面路径为 `/mnt/c/Users/lenovocloud/Desktop/`（注意实际用户名为 `lenovocloud`，非 `10153`）
